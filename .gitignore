# Go build artifacts
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Binaries and build folders
bin/
dist/
build/

# Air temp directory
/tmp/
tmp/

# IDE/editor settings
.vscode/
.idea/
*.code-workspace

# OS files
.DS_Store
Thumbs.db

# Node (not used, but ignore if present)
node_modules/

# Generated CSS (built by gotailwindcss from app/static/tailwind.input.css)
app/static/styles.css

# Coverage
coverage.out

# Local environment files (keep .env.example tracked)
.env
.env.*
!.env.example

# Built CLI/server binaries in repo root (cross-platform convenience)
gforge
server

# Logs and profiles
*.log
*.prof
profile.out

# Windows junk
Desktop.ini

# Templ generated files (always re-generated by gforge dev/test/build/export)
app/templates/*_templ.go

# Optional sample/demo content (kept locally, not tracked)
# About page
app/templates/about.templ
app/routes/about_route.go
app/ssg/register_about.go

# Auth scaffold
app/routes/auth_routes.go
app/templates/auth_*.templ
app/db/users.go

# Example API route
app/routes/example_api.go

# DB starter migration (will be created by `gforge add db` when needed)
app/db/migrations/0001_init.sql
