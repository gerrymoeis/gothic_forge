package templates

// Health page removed for the ultra-minimal starter. Keep a tiny placeholder
// component so the generated file remains consistent without affecting the app.
templ DeprecatedHealthNote() {
    <div class="hidden"></div>
}
